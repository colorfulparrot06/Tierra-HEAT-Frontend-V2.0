import React from "react";
import "../styles/GeneratedModel.css";

const GenerateModelButton = () => {
  return <button className="generate-model-button">Generate Model</button>;
};

export default GenerateModelButton;
